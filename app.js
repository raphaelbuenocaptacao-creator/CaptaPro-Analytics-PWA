(()=>{
'use strict';
const $=id=>document.getElementById(id),qs=(s,r=document)=>Array.from(r.querySelectorAll(s));
const state={rows:[],filtered:[],rank:'vgv',page:'visao'};
const money=n=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(n)||0),num=n=>new Intl.NumberFormat('pt-BR').format(Number(n)||0),pct=n=>((Number(n)||0)*100).toFixed(1).replace('.',',')+'%',norm=s=>String(s??'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim();
function val(o,keys){for(const k of keys)if(o&&o[k]!==undefined&&o[k]!==null&&o[k]!=='')return o[k];const nk=keys.map(norm);for(const k of Object.keys(o||{}))if(nk.includes(norm(k)))return o[k];return ''}
function number(v){if(typeof v==='number')return isFinite(v)?v:0;let s=String(v??'').trim();if(!s)return 0;s=s.replace(/R\$|\s/g,'');if(s.includes('.')&&s.includes(','))s=s.replace(/\./g,'').replace(',','.');else if(s.includes(','))s=s.replace(',','.');const n=Number(s.replace(/[^0-9.-]/g,''));return isFinite(n)?n:0}
function date(v){if(!v)return '';let s=String(v).trim(),m=s.match(/^(\d{2})[\/.-](\d{2})[\/.-](\d{4})/);if(m)return `${m[3]}-${m[2]}-${m[1]}`;m=s.match(/^(\d{4})[\/.-](\d{2})[\/.-](\d{2})/);return m?`${m[1]}-${m[2]}-${m[3]}`:''}
function normalize(o){return{raw:o,date:date(val(o,['data','date','Data','DATA','Data de atendimento'])),promotor:String(val(o,['promotor','Promotor','PROMOTOR','captador','Captador','Promotor de marketing'])||'').trim(),liner:String(val(o,['liner','Liner','LINER','consultor','Consultoria'])||'').trim(),closer:String(val(o,['closer','Closer','CLOSER'])||'').trim(),local:String(val(o,['local','Local','LOCAL','ponto','Ponto','Local Captação'])||'').trim(),vgv:number(val(o,['vgv','VGV','Vgv','valor_vgv','valorVgv','valor','Valor','venda','Venda','valor_venda','Valor vendido','_v'])),casal:number(val(o,['casais','Casais','casal','Casal','atendimentos','Atendimentos','atendimento','Atendimento']))||1,q:String(val(o,['q','Q','qualificado','Qualificado','qualificacao','Qualificação','Motivo de qualificação','_q'])||'').trim(),nq:String(val(o,['nq','NQ','nao_qualificado','Não Qualificado'])||'').trim(),tour:String(val(o,['tour','Tour','visita','Visita'])||'').trim(),city:String(val(o,['cidade','Cidade','CIDADE','Cidade do cliente','Município'])||'').trim(),profession:String(val(o,['profissao','Profissão','PROFISSÃO','ocupacao','Ocupação'])||'').trim(),income:String(val(o,['renda','Renda','RENDA','renda_familiar','Faixa de renda'])||'').trim(),age:String(val(o,['idade','Idade','IDADE'])||'').trim(),residence:String(val(o,['residencia','Residência','cidade_residencia','Local de residência'])||'').trim(),hour:String(val(o,['hora','Hora','HORÁRIO','horario','Horário','Hora de atendimento','horário de atendimento'])||'').trim()}}
function flatten(x){if(Array.isArray(x))return x.flatMap(flatten);if(x&&typeof x==='object'){const v=Object.values(x);if(v.every(z=>z&&typeof z==='object'&&!Array.isArray(z)))return v.flatMap(flatten);return[x]}return[]}
function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]||m))}
function opts(id,field){const e=$(id);if(!e)return;const a=[...new Set(state.rows.map(r=>r[field]).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'pt-BR'));e.innerHTML='<option value="">Todos</option>'+a.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join('')}
function buildFilters(){opts('promotor','promotor');opts('liner','liner');opts('closer','closer');opts('local','local');const d=state.rows.map(r=>r.date).filter(Boolean).sort();if(d.length){if($('start')&&!$('start').value)$('start').value=d[0];if($('end')&&!$('end').value)$('end').value=d[d.length-1]}qs('.section-filter').forEach(syncSection)}
function filterHTML(){return `<div class="section-filter filters"><div class="field"><label>PERÍODO</label><input data-filter="start" type="date"></div><div class="field"><label>ATÉ</label><input data-filter="end" type="date"></div><div class="field"><label>PROMOTOR</label><select data-filter="promotor"></select></div><div class="field"><label>LINER</label><select data-filter="liner"></select></div><div class="field"><label>CLOSER</label><select data-filter="closer"></select></div><div class="field"><label>LOCAL</label><select data-filter="local"></select></div><button type="button" class="btn section-clear">Limpar filtro da aba</button></div>`}
function injectFilters(){qs('section[id^="page-"]').forEach(s=>{if(!s.querySelector('.section-filter'))s.insertAdjacentHTML('afterbegin',filterHTML())});qs('.section-filter').forEach(b=>{b.addEventListener('change',e=>{const id=e.target.dataset.filter;if(!id)return;const m=$(id);if(m)m.value=e.target.value;apply()});b.querySelector('.section-clear')?.addEventListener('click',()=>{['start','end','promotor','liner','closer','local'].forEach(id=>{const e=$(id);if(e)e.value=''});buildFilters();apply()})})}
function syncSection(b){['start','end','promotor','liner','closer','local'].forEach(id=>{const s=$(id),d=b.querySelector(`[data-filter="${id}"]`);if(!s||!d)return;if(d.tagName==='SELECT')d.innerHTML=s.innerHTML;d.value=s.value||''})}
async function load(){try{const r=await fetch('./data.json?ts='+Date.now(),{cache:'no-store'});if(!r.ok)throw Error('HTTP '+r.status);const j=await r.json();state.rows=flatten(j).map(normalize).filter(r=>r.promotor||r.vgv||r.date||r.local);if(!state.rows.length)throw Error('Base sem registros');injectFilters();buildFilters();apply()}catch(e){console.error(e);qs('.kpi').forEach(x=>x.textContent='0');if($('highlight'))$('highlight').innerHTML='<div class="alert"><b>Erro ao carregar a base.</b><p>data.json não foi carregado ou não contém registros compatíveis.</p></div>'}}
function apply(){let a=state.rows.slice(),st=$('start')?.value,en=$('end')?.value,p=$('promotor')?.value,l=$('liner')?.value,c=$('closer')?.value,lo=$('local')?.value;if(st)a=a.filter(r=>!r.date||r.date>=st);if(en)a=a.filter(r=>!r.date||r.date<=en);if(p)a=a.filter(r=>r.promotor===p);if(l)a=a.filter(r=>r.liner===l);if(c)a=a.filter(r=>r.closer===c);if(lo)a=a.filter(r=>r.local===lo);state.filtered=a;qs('.section-filter').forEach(syncSection);render()}
function aggregate(a){const v=a.reduce((s,r)=>s+r.vgv,0),sales=a.filter(r=>r.vgv>0).length,atend=a.reduce((s,r)=>s+(r.casal>0?r.casal:1),0);return{v,sales,atend,conv:atend?sales/atend:0,ticket:sales?v/sales:0,per:atend?v/atend:0}}
function groups(a,f){const m=new Map();a.forEach(r=>{const k=r[f]||'Não informado';if(!m.has(k))m.set(k,[]);m.get(k).push(r)});return[...m].map(([name,rows])=>({name,rows,m:aggregate(rows)}))}
function table(a){if(!a.length)return'<div class="empty">Nenhum registro encontrado.</div>';return`<div class="tablewrap"><table><thead><tr><th>#</th><th>Promotor</th><th>VGV</th><th>Vendas</th><th>Conversão</th><th>Atendimentos</th></tr></thead><tbody>${a.map((x,i)=>`<tr><td>${i+1}</td><td>${esc(x.name)}</td><td class="money">${money(x.m.v)}</td><td>${num(x.m.sales)}</td><td>${pct(x.m.conv)}</td><td>${num(x.m.atend)}</td></tr>`).join('')}</tbody></table></div>`}
function render(){const a=state.filtered,m=aggregate(a);if($('vgv'))$('vgv').textContent=money(m.v);if($('casais'))$('casais').textContent=num(m.atend);if($('vendas'))$('vendas').textContent=num(m.sales);if($('conv'))$('conv').textContent=pct(m.conv);if($('ticket'))$('ticket').textContent=money(m.ticket);if($('vgvCasal'))$('vgvCasal').textContent=money(m.per);if($('vgvSub'))$('vgvSub').textContent=`${num(m.sales)} vendas · ${pct(m.conv)} conversão`;if($('convSub'))$('convSub').textContent=`Q: ${num(a.filter(r=>['q','qualificado','sim'].includes(norm(r.q))).length)} · NQ: ${num(a.filter(r=>['nq','nao','não','não qualificado'].includes(norm(r.q))).length)}`;if($('vgvProg'))$('vgvProg').style.width=Math.min(100,m.v/61700089.96*100)+'%';renderRanking();renderTeam();renderFunnel();renderHighlight();renderInsights();renderAlerts();renderOperation();renderProfile();renderData();drawDaily()}
function renderRanking(){let a=groups(state.filtered,'promotor'),k=state.rank==='vendas'?'sales':state.rank==='conv'?'conv':state.rank==='casais'?'atend':'v';a.sort((x,y)=>y.m[k]-x.m[k]);if($('rankingTable'))$('rankingTable').innerHTML=table(a.slice(0,30));if($('rankingFull'))$('rankingFull').innerHTML='<div class="info">Use os filtros desta aba para analisar o ranking por período, promotor, liner, closer ou local.</div>'+table(a)}
function renderTeam(){if($('teamTable'))$('teamTable').innerHTML=table(groups(state.filtered,'promotor'))}
function funnel(a){const m=aggregate(a),q=a.filter(r=>['q','qualificado','sim'].includes(norm(r.q))).length,nq=a.filter(r=>['nq','nao','não','não qualificado'].includes(norm(r.q))).length;return[['ABORDAGENS',m.atend,''],['QUALIFICADOS',q,'q'],['NÃO QUALIFICADOS',nq,'t'],['VENDAS',m.sales,'s']].map(x=>`<div class="funnelrow"><div class="funnelbar ${x[2]}" style="width:${Math.max(80,Math.min(100,x[1]/Math.max(1,m.atend)*100))}%">${x[0]}</div><div class="ftxt">${num(x[1])}</div></div>`).join('')}
function renderFunnel(){const h=funnel(state.filtered);if($('funnel'))$('funnel').innerHTML=h;if($('funnel2'))$('funnel2').innerHTML=h;if($('motives'))$('motives').innerHTML=groups(state.filtered,'q').sort((a,b)=>b.rows.length-a.rows.length).slice(0,20).map(g=>`<div class="stat"><span>${esc(g.name)}</span><b>${num(g.rows.length)}</b></div>`).join('')||'<div class="empty">Sem dados.</div>'}
function renderHighlight(){const e=$('highlight');if(!e)return;const g=groups(state.filtered,'promotor').sort((a,b)=>b.m.v-a.m.v)[0];e.innerHTML=g?`<div class="person"><div class="avatar">${esc(g.name.slice(0,1).toUpperCase())}</div><div><b>${esc(g.name)}</b><div class="badge">TOP VGV</div></div></div><div class="stats"><div class="stat">VGV <b>${money(g.m.v)}</b></div><div class="stat">Vendas <b>${num(g.m.sales)}</b></div><div class="stat">Conversão <b>${pct(g.m.conv)}</b></div><div class="stat">Atendimentos <b>${num(g.m.atend)}</b></div></div>`:'<div class="empty">Sem dados.</div>'}
function renderInsights(){const e=$('insights');if(e){const m=aggregate(state.filtered);e.innerHTML=`<div class="insight"><b>Resumo</b><p>${num(m.sales)} vendas em ${num(m.atend)} atendimentos, com conversão de ${pct(m.conv)} e VGV de ${money(m.v)}.</p></div><div class="info">Registros filtrados: <b>${num(state.filtered.length)}</b></div>`}}
function renderAlerts(){const e=$('alerts');if(e){const m=aggregate(state.filtered);e.innerHTML=m.conv<.2?'<div class="alert"><b>⚠️ Conversão abaixo de 20%.</b><p>Revise abordagem e qualificação.</p></div>':'<div class="info">✅ Conversão acima de 20%.</div>'}}
function draw(id,pts,label){const c=$(id);if(!c)return;const ratio=devicePixelRatio||1,w=c.clientWidth||600,h=c.clientHeight||275;c.width=w*ratio;c.height=h*ratio;const x=c.getContext('2d');x.setTransform(ratio,0,0,ratio,0,0);x.clearRect(0,0,w,h);if(!pts.length){x.fillStyle='#8d99aa';x.font='14px sans-serif';x.fillText('Sem dados para o período',20,35);return}const max=Math.max(1,...pts.map(p=>p.v)),pad=35;x.strokeStyle='#1683ff';x.lineWidth=2;x.beginPath();pts.forEach((p,i)=>{const xx=pad+(w-pad*2)*i/Math.max(1,pts.length-1),yy=h-pad-(h-pad*2)*p.v/max;i?x.lineTo(xx,yy):x.moveTo(xx,yy)});x.stroke();x.fillStyle='#8d99aa';x.font='10px sans-serif';x.fillText(label,pad,16)}
function drawDaily(){const m={};state.filtered.forEach(r=>{const d=r.date||'Sem data';m[d]=(m[d]||0)+r.vgv});draw('daily',Object.keys(m).sort().map(d=>({v:m[d]})),'VGV por dia')}
function renderOperation(){const c=$('hours');if(c){const m={};state.filtered.forEach(r=>{const z=String(r.hour||'').match(/\d{1,2}/);if(z){const h=String(Math.min(23,+z[0])).padStart(2,'0');m[h]=(m[h]||0)+r.vgv}});draw('hours',Object.keys(m).sort((a,b)=>+a-+b).map(k=>({v:m[k]})),'VGV por hora')}if($('locations'))$('locations').innerHTML=groups(state.filtered,'local').sort((a,b)=>b.m.v-a.m.v).slice(0,20).map(g=>`<div class="stat"><span>${esc(g.name)}</span><b>${money(g.m.v)}</b></div>`).join('')||'<div class="empty">Sem dados.</div>'}
function list(a){return a.length?a.map(g=>`<div class="stat"><span>${esc(g.name)}</span><b>${num(g.rows.length)}</b></div>`).join(''):'<div class="empty">Sem dados preenchidos na base.</div>'}
function renderProfile(){const e=$('profileTable');if(!e)return;const city=groups(state.filtered,'city').sort((a,b)=>b.rows.length-a.rows.length).slice(0,20),prof=groups(state.filtered,'profession').sort((a,b)=>b.rows.length-a.rows.length).slice(0,20),inc=groups(state.filtered,'income').sort((a,b)=>b.rows.length-a.rows.length).slice(0,20);e.innerHTML=`<div class="metricgrid"><div class="metricbox">Cidades<strong>${num(city.filter(x=>x.name!=='Não informado').length)}</strong></div><div class="metricbox">Profissões<strong>${num(prof.filter(x=>x.name!=='Não informado').length)}</strong></div><div class="metricbox">Rendas<strong>${num(inc.filter(x=>x.name!=='Não informado').length)}</strong></div></div><div class="grid"><div class="card s4"><div class="title">Cidades</div>${list(city)}</div><div class="card s4"><div class="title">Profissões</div>${list(prof)}</div><div class="card s4"><div class="title">Renda</div>${list(inc)}</div></div>`}
function renderData(){const e=$('dataTable');if(e)e.innerHTML=`<div class="info"><b>${num(state.filtered.length)}</b> registros filtrados de <b>${num(state.rows.length)}</b> carregados.</div>`}
function nav(page){state.page=page;qs('[id^="page-"]').forEach(x=>x.hidden=x.id!==`page-${page}`);qs('.nav button,.mobilebar button').forEach(b=>b.classList.toggle('active',b.dataset.page===page));const t={visao:'Visão 360°',equipe:'Equipe',funil:'Funil de Vendas',ranking:'Ranking',operacao:'Operação',perfil:'Perfil do Cliente',alertas:'Alertas',dados:'Dados'};if($('pageTitle'))$('pageTitle').textContent=t[page]||'FluxoHub';setTimeout(()=>qs('.section-filter').forEach(syncSection),0)}
function bind(){document.addEventListener('click',e=>{const n=e.target.closest('[data-page]');if(n){e.preventDefault();nav(n.dataset.page);return}const r=e.target.closest('[data-rank]');if(r){e.preventDefault();state.rank=r.dataset.rank;qs('[data-rank]').forEach(x=>x.classList.toggle('active',x===r));renderRanking()}});['start','end','promotor','liner','closer','local'].forEach(id=>$(id)?.addEventListener('change',apply));$('reset')?.addEventListener('click',()=>{['start','end','promotor','liner','closer','local'].forEach(id=>{const e=$(id);if(e)e.value=''});buildFilters();apply()});$('importBtn')?.addEventListener('click',()=>$('file')?.click());$('file')?.addEventListener('change',async e=>{const f=e.target.files?.[0];if(!f)return;const text=await f.text(),lines=text.split(/\r?\n/).filter(Boolean),sep=lines[0].includes(';')?';':',',heads=lines[0].split(sep).map(x=>x.replace(/^"|"$/g,''));state.rows=lines.slice(1).map(line=>{const c=line.split(sep),o={};heads.forEach((h,i)=>o[h]=(c[i]||'').replace(/^"|"$/g,''));return normalize(o)});buildFilters();apply()});$('export')?.addEventListener('click',()=>{const csv=['PROMOTOR,VGV,VENDAS,CONVERSAO,ATENDIMENTOS',...groups(state.filtered,'promotor').map(g=>[JSON.stringify(g.name),g.m.v,g.m.sales,(g.m.conv*100).toFixed(2),g.m.atend].join(','))].join('\n');const u=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'})),a=document.createElement('a');a.href=u;a.download='fluxohub-export.csv';a.click();URL.revokeObjectURL(u)})}
function init(){bind();nav('visao');load()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();